apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: konstfish-ui
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: main
  paths:
  - gitops/apps/ui-konst-fish
  targets:
  - clusterName: tetra