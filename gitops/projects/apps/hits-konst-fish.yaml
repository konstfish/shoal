apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: hits-konst-fish
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: main
  paths:
  - gitops/apps/hits-konst-fish
  targets:
  - clusterName: tetra