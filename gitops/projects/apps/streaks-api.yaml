apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: streaks-api
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: main
  paths:
  - gitops/apps/streaks-api
  targets:
  - clusterName: guppy