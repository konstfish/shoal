apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: operators
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: gitopsv2
  paths:
  - gitops/operators
  targets:
  - clusterName: barracuda
  - clusterName: tetra
  - clusterName: guppy
  - clusterName: anchovy
  insecureSkipTLSVerify: false
  correctDrift:
    enabled: false
---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: operators-local
  namespace: fleet-local
spec:
  repo: https://github.com/konstfish/shoal
  branch: gitopsv2
  paths:
  - gitops/operators
  insecureSkipTLSVerify: false
  correctDrift:
    enabled: false