apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: knative
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: main
  paths:
  - gitops/infra/knative/base
  targets:
  - clusterName: tetra