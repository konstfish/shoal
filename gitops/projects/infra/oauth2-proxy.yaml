apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: oauth2-proxy
  namespace: fleet-default
spec:
  repo: https://github.com/konstfish/shoal
  branch: main
  paths:
  - gitops/infra/oauth2-proxy
  targets:
  - clusterName: tetra