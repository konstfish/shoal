apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempo-ingester-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tempo-ingester-frontend
  template:
    metadata:
      labels:
        app: tempo-ingester-frontend
    spec:
      serviceAccountName: tempo-kube-rbac-proxy
      containers:
      - name: kube-rbac-proxy
        image: quay.io/brancz/kube-rbac-proxy:v0.16.1
        args:
          - "--secure-listen-address=0.0.0.0:4317"
          - "--upstream=http://tempo-tempo-distributor:4317"
          - "--config-file=/etc/kube-rbac-proxy/config.yaml"
          - "--logtostderr=true"
          - "--v=10"
        ports:
          - containerPort: 4317
            name: https
        volumeMounts:
          - name: config
            mountPath: /etc/kube-rbac-proxy
        resources:
          requests:
            memory: 256Mi
            cpu: 150m
          limits:
            memory: 1Gi
            cpu: 500m
      volumes:
        - name: config
          configMap:
            name: tempo-kube-rbac-proxy