namespace: pocketbase

targetCustomizations:
  - name: sudoku
    clusterName: tetra
    kustomize:
      dir: overlays/sudoku
  - name: cards
    clusterName: tetra
    kustomize:
      dir: overlays/cards